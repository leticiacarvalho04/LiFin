import { Text, View } from "react-native";
import Layout from "../../components/layout";

export default function PainelMetas() {
    return (
        <Layout>
            <View>
                <Text>Painel de Metas</Text>
            </View>
        </Layout>
    );
}